## 0

fix: 403 => force logout user on client

fix: correct reflect if logout other tab (probably LOGOUT on empty LS)

fix: ** save & exit after server restart anyway goes to users list

i: make <LoggedUser> component (will show logged user, will load it from localStorage OR cookie)

i: make flex css & breadcrumbs


## last:

try clone&check project

remove unneeded files & comments

remove console.log from code

increase getLoggedUserNextRefreshTime interval (1 minute)

review dev/common dependencies

## 1

verify: if it's ok to store session in memory on server (maybe for demo ok?)

r: don't overwrite action payload completely by error response, save sent to server data somewhere. Maybe payload->req, ->response?

+: when leave create user page with unsaved user show warning


## 2

fix: make localStorage logged user obsolete time & get it from server

fix: add favicon, static files

+: implement "are you sure" when exi

r: getUserError should return list of error, not string

i: favicon -> processing static files

+ activate user name input control initially

+: move setUtil to external module

+: multilanguage

+: add functional & rest-server tests

+: use react-forms and compare

fix: redirect to create-user page BACK after logging in (see nextPathname)